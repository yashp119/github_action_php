name: Build and SonarQube Scan

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build and SonarQube Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      # You can add your build steps here if needed

      - name: Create SonarQube Analysis Report
        run: |
          # Placeholder command for creating SonarQube analysis report
          echo "Mock SonarQube report generation step"
          # You can replace the above line with the actual command to generate the SonarQube report

      - name: Create email body
        run: echo "SonarQube report is ready" > email-body.txt

      - name: Send Email
        uses: dawidd6/action-send-mail@v2
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.GMAIL_USER }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: SonarQube Report
          body: email-body.txt
          to: niharika.kadellabs@gmail.com
          from: ${{ secrets.GMAIL_USER }}

# You can add additional steps or jobs as needed for your specific build and deployment process
